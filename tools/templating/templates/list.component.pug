// ##fullName## pug

h2 ##entityName##

h3(*ngIf="!items || items.length==0") No items resolved

table.table.table-hover
  thead
    tr
      th Id
      th Name
  tbody
    tr(*ngFor="let item of items", (click)="goToDetail(item)")
      td {{item._id}}
      td {{item.name}}
